# Backend API base URL — wire this up when connecting to the FastAPI backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# Gradio apps — TESTING ONLY, used by /gradio-test to preview the Python
# Gradio frontend inside Next.js via iframe. Remove this page before
# shipping to production; it exists purely so you can sanity-check the
# Gradio UI while both frontends exist side by side.
NEXT_PUBLIC_GRADIO_PATIENT_URL=http://localhost:7860
NEXT_PUBLIC_GRADIO_ADMIN_URL=http://localhost:7861
